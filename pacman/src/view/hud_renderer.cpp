#include "hud_renderer.hpp"

void HudRenderer::render(SDL_Renderer* renderer, ResourceManager& res,
                          const Score& score) {
    // TODO
    (void)renderer;
    (void)res;
    (void)score;
}
