#include "stage_renderer.hpp"

void StageRenderer::render(SDL_Renderer* renderer, ResourceManager& res,
                            const StageData& stage, const Player& player,
                            const Ghost ghosts[4]) {
    // TODO
    (void)renderer;
    (void)res;
    (void)stage;
    (void)player;
    (void)ghosts;
}
