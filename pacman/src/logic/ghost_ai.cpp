#include "ghost_ai.hpp"

Direction GhostAI::decide(const Ghost& ghost, const Player& player,
                           const Map& map) {
    // TODO: BFS 最短路径追踪
    (void)ghost;
    (void)player;
    (void)map;
    return Direction::None;
}
